a
  color inherit
  text-decoration none
  border-bottom 3px solid interacted
  box-shadow inset 0 -4px 0 interacted
  transition box-shadow timing easing
  &:hover
    color inherit
    box-shadow inset 0 -24px 0 interacted
#skip-to-content
  padding 1em
  position absolute
  top -40px
  left 1em
  z-index 1000
  color white
  &:focus
    top 0
    background #4593ff